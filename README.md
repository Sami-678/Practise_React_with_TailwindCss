---------------To Use This project-----------------------

1. first you have to install the node modules
2. then yuo have to run the project

                 npm install
                 npm run dev

----------------Things you should know about --------------------

1. first you must have NodeJS installed in your system
 
------------How to start react---------- 

. react is a library , it is compulsory either to make mobile app or web app 
. react-dom for web
. react-native for mobile app 

----------- methods to make react project or to use react------------------- 


  1. First Method to install React 

. type :
------
                  npx 
-----
. as we know that there is NPM , which comes in by default when we install node js , here NPX stands for Node Package Execuiter , which executes instead of installing it 

. after npx type :
---------------
                npx create-react-app 01basicreact 
---------------
. basically we are telling the terminal to execute our utility (create-react-app) , and the name we want to gave it is : 01basicreact

  --------------- 2. Second Method to install React ---------------

.also we can run using vite and 


  -------------3. run the file and check the details--------------

. as we are in the main folder , so we have to go to the real file , so that we have to type 

-------------
                cd 01basicreact 
-------------

. here we execute the code 
. first open package.json
. if we go in package.json , we can easily see :
        1. name : (name of file or project)
        2. version of that file or project
        3. dependencies : there are two main dependencies 
                          as first is react and second is react-dom
        
  -------------4. Scripts

. these scripts are responsible for running the project 
. there are 4 scripts :
        start : to start the project 
        build : to generate the code that is understandable 
                by browser 
        test : to test 
        eject : to stop the work , due to move on another 
                project

 ------------ 5. run the file --------------- 

. in order to run the file we have to type 

. as we type 
---------
               cd 01basicreact
---------

. then we have to type 
--------
                   dir 
-------

. this shows all the directory , in these we must have to saw package.json

. as we have to use script start , so we have to type :
---------
                npm run start 
---------

